# 🧭 Числяндия — Рабочий компас (Web MVP)

> Обновляй этот файл после каждой сессии.  
> Правило: один чекбокс = одна законченная задача.

**Проект:** `C:\Users\Fireboy\Dev\Projects\Chislyandia`  
**Старт спринта:** 2026-06-16  
**Дедлайн:** 2026-06-20 (5 дней)  
**Фокус:** Web MVP + тесты + staging (TG уже в полевых тестах)

---

## 🎯 Цель спринта

Собрать **играбельный web**, который работает на той же БД/ядре, что и Telegram:
- меню → задачи → очки
- банк / замок / артефакты
- алхимия (минимум)
- staging для демо

**Не цель спринта:** полный паритет с TG (боссы, карта миров, тайная комната → v2).

---

## 📍 Где мы сейчас (снимок)

| Зона | Статус | Комментарий |
|------|--------|-------------|
| Telegram бот | ✅ Готов к полевым тестам | `manyunya_bot.py` v4.8 |
| Flask API | 🟡 Частично | `web/api_server.py` — core endpoints есть |
| React frontend | 🟡 В работе | 7 экранов, связка чинится |
| API ↔ Frontend | 🟡 Почти готово | единый `botAdapter.js`, порт 5000 |
| Алхимия web | 🔴 Нет API | UI есть, `craftAlchemy` отсутствует |
| Тесты | 🔴 Нет | Нужны smoke на API |
| Staging | 🔴 Не настроен | Timeweb доступы не зафиксированы |

**Текущий этап:** `День 4 — smoke-тесты API, стабилизация`  
**Следующий шаг:** staging (День 5) или ручной регресс-чеклист

---

## ✅ Definition of Done (web «готово»)

- [ ] Один API-адаптер во фронте (`botAdapter.js`)
- [ ] Один порт / `VITE_API_URL` везде согласован
- [ ] TaskScreen грузит реальный профиль (без мок-статов)
- [ ] Меню → задача → ответ → баланс обновляется в SQLite
- [ ] Банк: вклад и снятие работают
- [ ] Замок: upkeep + улучшение декорации
- [ ] Артефакты: просмотр + апгрейд
- [ ] Алхимия: крафт через API (не mock)
- [ ] `userId` не захардкожен в `App.jsx`
- [ ] `npm run build` проходит
- [ ] 5–10 pytest smoke на API
- [ ] Staging поднят и открывается по URL
- [ ] Ручной чеклист пройден без P0/P1

---

## 🗓️ План по дням

### День 1 — Связка frontend ↔ API (КРИТИЧНО)
- [x] Создать `docs/WORK_COMPASS.md`
- [x] Убрать дубли: TaskScreen на `botAdapter.js` (не `Adapter.js`)
- [x] Порт: `5000` везде (или единый `VITE_API_URL`)
- [x] TaskScreen: загрузка профиля при старте
- [x] TaskScreen: убрать хардкод `level/coins/hearts/xp`
- [x] `userId` из URL в `App.jsx`
- [x] Профиль API: `defeated_bosses`, `unlocked_zones` для экранов
- [ ] Проверка E2E локально: меню → задача → ответ → баланс

**Критерий дня:** web играет на реальной БД, не на моках.

---

### День 2 — Экономика
- [x] CastleScreen: реальный `upgradeDecoration` (без mock-fallback)
- [x] ArtifactsScreen: правильный парсинг уровня артефактов
- [x] botAdapter: write-операции не маскируют ошибки API
- [x] BankScreen: показ ошибки если игрок не найден
- [ ] Ручной прогон: банк / замок / артефакты

**Критерий дня:** 3 экономических экрана стабильны.

---

### День 3 — Алхимия
- [x] `craft_alchemy_item()` — синхронный крафт (общий с TG)
- [x] API: `POST /api/alchemy/:user_id/craft`
- [x] `botApi.craftAlchemy()` во фронте
- [x] AlchemyScreen без mock-success
- [ ] Ручной прогон: крафт зелья (если рецепт открыт)

**Критерий дня:** можно купить/созать зелье и увидеть изменение в профиле.

---

### День 4 — Тесты и стабилизация
- [x] pytest: health, profile, task, answer, bank, castle, alchemy (11 smoke)
- [x] `scripts/run-tests.ps1`
- [x] `npm run build` без ошибок
- [ ] `npm run lint` — 22 pre-existing ошибки (Chaos-компоненты, botAdapter)
- [ ] Ручной регресс-чеклист (см. ниже)
- [ ] Убрать/ограничить silent mock-fallback (чтобы не маскировал баги)

**Критерий дня:** есть автотесты + зелёный build.

---

### День 5 — Staging
- [ ] Зафиксировать доступы Timeweb (SSH, домен, порты)
- [ ] Деплой API на VPS
- [ ] Деплой frontend static
- [ ] `VITE_API_URL` на staging
- [ ] Финальный прогон чеклиста на staging URL

**Критерий дня:** ссылка staging открывается и проходит сценарии.

---

## 🧪 Ручной чеклист

```
[ ] API health: GET /api/health → ok
[ ] Профиль грузится на MenuScreen
[ ] Получить задачу на TaskScreen
[ ] Правильный ответ → +очки, баланс изменился
[ ] Неправильный ответ → штраф/хаос отображается
[ ] Банк: вклад 100+ работает
[ ] Банк: снятие работает
[ ] Замок: upkeep оплачивается
[ ] Замок: декорация улучшается
[ ] Артефакт: апгрейд работает (при активном upkeep)
[ ] Алхимия: крафт работает
[ ] После F5 данные не «откатываются» в мок
```

---

## 🚫 Блокеры

| # | Блокер | Статус | Кто решает |
|---|--------|--------|------------|
| 1 | Два API-адаптера + разные порты | 🟡 В работе | Мы (День 1) |
| 2 | Нет API для алхимии | 🔴 Открыт | Мы (День 3) |
| 3 | Timeweb доступы не описаны | 🟡 Ждём | Ты |
| 4 | Нет автотестов | 🔴 Открыт | Мы (День 4) |

---

## 🧠 Технический долг (не в этом спринте)

- [ ] Web-экран боссов
- [ ] Карта миров / выбор острова в TaskScreen
- [ ] Инвентарь
- [ ] Тайная комната
- [ ] Настройки/звук (кнопки-заглушки в TaskScreen)
- [ ] Обновить старый `ROADMAP.md` (устарел)

---

## 📝 Журнал сессий

### 2026-06-16 — Сессия 3 (День 4: тесты)
**Сделано:**
- `tests/test_api_smoke.py` — 11 smoke на API (изолированная temp-БД)
- Фиксы по пути: банк через `get_db_path()`, штраф за ошибку (был +25 вместо -25), откат баланса в `solve_task`, `castle_data` миграция, `upkeep_paid_until=None` в замке

**Запуск:** `.\scripts\run-tests.ps1` или `python -m pytest tests/ -v`

---
**Сделано:**
- Задачи убраны из web: `core/task_loader.py` + `data/worlds/` (как TG)
- `api_server.py` только прокси: `engine.get_random_task()` / `resolve_task_answer()`
- Сравнение ответов как в TG (числовое, не только строка)
- Починена задача-перенос для формата «Сколько будет 27 + 14?»
- TaskScreen шлёт активную задачу (включая transfer)

**Проверить:** перезапусти API → задача «27+14» → ответ `41` → должно быть ✅

---

## 🎛 Режимы работы с AI

| Задача | Режим |
|--------|-------|
| План, приоритеты, обсуждение | Без агент-режима |
| Правки кода, запуск API, тесты, деплой | Агент-режим |

**Текущая рекомендация:** агент-режим для Дня 1–5.
